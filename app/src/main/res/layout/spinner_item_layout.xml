<?xml version="1.0" encoding="utf-8"?>
<jp.co.fujixerox.sa.ion.views.SpinnerItemView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical">

    <LinearLayout
        android:id="@+id/labelLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:textColor="@color/item_label_text_color">

        <TextView
            android:id="@+id/textViewItemLabel"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:singleLine="true"
            android:text="@android:string/untitled" />

        <ImageView
            android:id="@+id/checkItemLabel"
            android:layout_width="20dp"
            android:layout_height="20dp"
            android:src="@drawable/checked"
            android:visibility="gone" />
    </LinearLayout>
    <!-- SELECT ITEM -->
    <RelativeLayout
        android:id="@+id/spinnerItemTop"
        android:layout_width="match_parent"
        android:layout_height="@dimen/item_height"
        android:layout_marginBottom="@dimen/activity_vertical_margin">
        <Spinner
            android:id="@+id/spinnerItem"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@drawable/input_item_bg"
            android:layout_alignParentTop="true"
            android:layout_alignParentLeft="true" />
        <ImageView
            android:id="@+id/ivSpinnerChecked"
            android:layout_width="@dimen/checked_icon_size"
            android:layout_height="@dimen/checked_icon_size"
            android:layout_alignParentRight="true"
            android:layout_centerInParent="true"
            android:layout_marginRight="5dp"
            android:contentDescription="@string/app_name"
            android:src="@drawable/check"
            android:tint="@color/red_200"
            android:visibility="gone" />

    </RelativeLayout>
    <LinearLayout
        android:id="@+id/childLayout"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">
    </LinearLayout>
</jp.co.fujixerox.sa.ion.views.SpinnerItemView>